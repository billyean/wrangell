## Start miYe application

* Windows
    ```shell
    (venv) $env:FLASK_APP = "app.py"
    (venv) flask run
    ```
    
* Mac/Linux
    ```shell
    (venv) export FLASK_APP=app
    (venv) flask run
    ```